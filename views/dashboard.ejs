<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chart Example</title>
</head>
<body>

    <canvas id="myChart" style="width:100%;max-width:600px"></canvas>
    <canvas id="myChart2" style="width:100%;max-width:600px"></canvas>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>
    <script>
        fetch('data.json')
        .then(response => response.json())
        .then(products => {
            // Count items per category
            const categoryCounts = products.reduce((acc, product) => {
            const category = product.Category;
            acc[category] = (acc[category] || 0) + 1;
            return acc;
            }, {});

            // Extract categories and counts into arrays
            const xValues = Object.keys(categoryCounts);
            const yValues = Object.values(categoryCounts);
            const barColors = ["blue", "orange", "red", "green", "yellow", "purple", "pink", "brown"];

            // Create Bar Chart
            new Chart("myChart", {
            type: "bar",
            data: {
                labels: xValues,
                datasets: [{
                backgroundColor: barColors,
                data: yValues
                }]
            },
            options: {
                legend: { display: false },
                title: {
                display: true,
                text: "Products by Category"
                },
                scales: {
                yAxes: [{
                    ticks: {
                    beginAtZero: true,
                    precision: 0
                    }
                }]
                }
            }
            });

            // Create Pie Chart
            new Chart("myChart2", {
            type: "pie",
            data: {
                labels: xValues,
                datasets: [{
                backgroundColor: barColors,
                data: yValues
                }]
            },
            options: {
                title: {
                display: true,
                text: "Products by Category (Pie)"
                }
            }
            });
        })
        .catch(error => console.error('Error loading data:', error));
    </script>
</body>
</html>
